<?xml version="1.0" encoding="utf-8"?>
<!-- Watched-state toggle: green-filled circle when checked (selected), hollow
     outline pill otherwise. Shared by the episode-row check and the season-chip
     check, so both read as the same "mark watched" affordance. Selected state
     comes from isSelected (not a Checkable), so focus keeps its own ring. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true" android:state_focused="true">
        <shape android:shape="oval">
            <solid android:color="@color/success_green" />
            <stroke android:width="2dp" android:color="@color/focus" />
        </shape>
    </item>
    <item android:state_selected="true">
        <shape android:shape="oval">
            <solid android:color="@color/success_green" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape android:shape="oval">
            <solid android:color="@color/surface_emphasis" />
            <stroke android:width="2dp" android:color="@color/focus" />
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/surface_elevated" />
            <stroke android:width="1dp" android:color="@color/color_border" />
        </shape>
    </item>
</selector>
